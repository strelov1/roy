VITE_ROY_WS_URL=ws://127.0.0.1:8788
# Auth is JWT-based: users sign in through the in-app form, /auth/login
# sets an HttpOnly cookie, and the JS layer reads the JWT from the same
# response's `ws_token` field to pass into the WebSocket subprotocol.
# No env var is needed for authentication.
